const producto7 = {
    nombre : "Monitor de 20 pulgadas",
    precio: 300,
    disponible: true,
  
}

console.log(Object.keys(producto7)); // retorna llaves

console.log(Object.values(producto7)); // retorna valores

console.log(Object.entries(producto7)); // retorna todo