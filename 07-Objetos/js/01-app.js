// Creacion de objetos

const nombre = 'Monitor de 20 pulgadas';
const precio = 500;
const disponible = true;

// El obnjeto agrupa todas estas variables.

// Object literal
const producto = {
    nombre:'Monitor de 20 pulgadas',
    precio:500,
    disponible:true
}

console.log(producto);